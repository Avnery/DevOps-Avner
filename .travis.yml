sudo: required

language: python

python:
  - "2.7"

services:
  - docker
  
before_install:
- docker build -t dynamoextractor -f .\Dockerfile .
- docker run -d -p 127.0.0.1:5000:5000 dynamoextractor

script:
  - bash test.sh
